"async function showLiquidationModal() {\n    const data = window.activeGameData || window.historyGameData;\n    if (!data) return;\n\n    const pIds = Object.keys(data.players);\n    const balances = {};\n    pIds.forEach(id => balances[id] = 0);\n\n    // Usar la lista de modos del engine o la configurada en la partida\n    const activeModes = data.modes || (data.selectedModes) || [data.mode || 'Wolf'];\n    const modeBets = data.modeBets || {};\n\n    activeModes.forEach(mode => {\n        // Obtenemos puntos para ESTE modo especÃ­fico\n        const { pointsTable, modeHoleDetails } = window.GolfEngine.calculateTotalPoints(data, mode);\n\n        if (mode === 'Medal') {\n            const betData = modeBets[mode] || {};\n            const betF9 = parseFloat(betData.f9 || 0);\n            const betB9 = parseFloat(betData.b9 || 0);\n            const betT18 = parseFloat(betData.t18 || 0);\n\n            const medalSections = [\n                { bet: betF9, range: [0, 9] },\n                { bet: betB9, range: [9, 18] },\n                { bet: betT18, range: [0, 18] }\n            ];\n\n            medalSections.forEach(section => {\n                if (section.bet <= 0) return;\n                const sectionData = pIds.map(pid => {\n                    const strokes = (modeHoleDetails && modeHoleDetails[pid]?.holes) || [];\n                    const total = strokes.slice(section.range[0], section.range[1]).filter(s => s > 0).reduce((a, b) => a + b, 0);\n                    return { pid, total };\n                }).filter(d => d.total > 0);\n\n                if (sectionData.length > 1) {\n                    sectionData.sort((a, b) => a.total - b.total);\n                    const sTop = sectionData[0].total;\n                    const sWinners = sectionData.filter(d => d.total === sTop);\n                    const losers = sectionData.filter(d => d.total > sTop);\n                    const winAmount = (losers.length * section.bet) / sWinners.length;\n\n                    sectionData.forEach(d => {\n       
<truncated 2749 bytes>
